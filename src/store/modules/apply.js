const state = {
    applies: []
  }
  
  const getters = {}
  
  const actions = {}

  const mutations = {
    setApply (state, data) {
      state.applies = data;
    }
  }
  
  export default {
    state,
    getters,
    actions,
    mutations
  }