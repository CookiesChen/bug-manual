const state = {
    schools: []
  }
  
  const getters = {}
  
  const actions = {}

  const mutations = {
    setSchool (state, data) {
      state.schools = data;
    }
  }
  
  export default {
    state,
    getters,
    actions,
    mutations
  }